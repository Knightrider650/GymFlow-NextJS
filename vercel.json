{
  "framework": "nextjs",
  "buildCommand": "prisma db push --accept-data-loss && prisma db seed && npx prisma generate && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "regions": ["cle1"],
  "cleanUrls": true
}
